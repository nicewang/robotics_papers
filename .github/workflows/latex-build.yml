name: Build LaTeX PDF

on:
  push:
    branches: [no_exist]
  #   branches: [ "journal_tro_24_port_hamiltonian_ode" ]
  # pull_request:
  #   branches: [ "journal_tro_24_port_hamiltonian_ode" ]

jobs:
  build_latex:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Compile LaTeX document
        uses: xu-cheng/latex-action@v3
        with:
          root_file: main.tex
          # root_file: journal/TRO/24/Port-Hamiltonian-ODE/main.tex
          working_directory: journal/TRO/24/Port-Hamiltonian-ODE/

      # - name: Upload PDF artifact
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: paper-summary-pdf
      #     path: main.pdf
      
      - name: Rename PDF
        run: |
          old_path="journal/TRO/24/Port-Hamiltonian-ODE/main.pdf"
          new_name="Port-Hamiltonian-ODE_summarization.pdf"
          mv "$old_path" "$new_name"

      - name: Upload PDF artifact
        uses: actions/upload-artifact@v4
        with:
          name: Port-Hamiltonian-ODE_summarization_pdf
          path: Port-Hamiltonian-ODE_summarization.pdf


#  Orig Ver
# name: Build LaTeX PDF

# on:
#   push:
#     # branches: [ "main" ]
#     branches: [ "no_exist" ]
#   # pull_request:
#     # branches: [ "main" ]

# jobs:
#   build_latex:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Compile LaTeX document
#         uses: xu-cheng/latex-action@v3
#         with:
#           root_file: main.tex

#       # - name: Upload PDF artifact
#       #   uses: actions/upload-artifact@v4
#       #   with:
#       #     name: paper-summarization-template-pdf
#       #     path: main.pdf
      
#       - name: Rename PDF
#         run: |
#           old_path="main.pdf"
#           new_name="paper_summarization_template.pdf"
#           mv "$old_path" "$new_name"

#       - name: Upload PDF artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: paper-summarization-template-pdf
#           path: paper_summarization_template.pdf